#pragma once

// namespace for physical robot and field dimensions
namespace dimensions {
constexpr float GOAL_HEIGHT = 74.93f;  // 29.5 in to cm
constexpr float LAUNCH_ANGLE = 0.785398f;  // 45 deg to radians
constexpr float LAUNCH_HEIGHT = 38.1f;  // 15 in to cm
constexpr float LENS_ANGLE = 0.4276f;  // 24.5 deg to radians
constexpr float SENSOR_HEIGHT = 43.18f;  // 17 in to cm
}  // namespace dimensions
