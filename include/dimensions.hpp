#pragma once

// namespace for phsyical robot and field dimensions
namespace dimensions {
constexpr float GOAL_HEIGHT = 63.5;  // 25 in to cm
constexpr float LAUNCH_ANGLE = 0.785398f;  // 45 deg to radians
constexpr float LAUNCH_HEIGHT = 38.1f;  // 15 in to cm
constexpr float LENS_ANGLE = 0.261799f;  // 15 deg to radians
constexpr float SENSOR_HEIGHT = 44.45f;  // 17.5 in to cm
}  // namespace dimensions
